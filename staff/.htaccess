# Enable rewrite engine
RewriteEngine On

# Redirect /staff/index.html → /staff/
RewriteCond %{THE_REQUEST} ^[A-Z]{3,}\s+index\.html[\s?] [NC]
RewriteRule ^index\.html$ ./ [R=301,L]

# Redirect /staff (no slash) → /staff/
RewriteCond %{REQUEST_URI} ^/staff$ [NC]
RewriteRule ^$ ./ [R=301,L]

